{
  "kind": "code_review",
  "rung": "senior",
  "locked": false,
  "expected": "reject",
  "principle": "unstated_prerequisite",
  "section": {
    "question": "What's the issue with this rate-limit counter, and how would you fix it?",
    "snippet": "class RateLimitCache\n  def initialize\n    @counts = {}\n  end\n\n  def increment(key)\n    @counts[key] ||= 0\n    @counts[key] += 1\n  end\n\n  def record_request(key)\n    Thread.new { increment(key) }\n  end\nend\n\ncache = RateLimitCache.new\n10.times { cache.record_request(\"user:42\") }",
    "teaching_note": "Look at how many requests actually land in @counts once all ten calls finish.",
    "concept": "n_plus_one",
    "scenario": "a per-user API rate limiter",
    "diagram": ""
  }
}
