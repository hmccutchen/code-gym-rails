<%# plan_excerpt goes through glossary_wrap like every other prose field in this
    app — it's prose, not code, so there's no reason to withhold tooltips from
    it specifically. Locals: data, exercise, response, submitted. %>
<div class="plan-excerpt"><%= glossary_wrap(data["plan_excerpt"]) %></div>
